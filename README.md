# Business Rules

## How to run:

### Running with Docker:

Build image:

``` $ docker-compose build```

Run container:

``` $ docker-compose up```

Run migrations:

``` docker-compose exec business_rules python manage.py migrate```

